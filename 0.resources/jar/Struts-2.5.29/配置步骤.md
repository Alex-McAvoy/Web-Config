# Struts2 配置步骤

1. 导入 `jar` 包
2. 更改 `/WEB-INF/web.xml`
3. 更改 `/src/struts.xml` 文件

---

> 使用 `struts2` 标签的 `taglib` 指令：
>
> ```xml
> <%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core"%>
> ```

