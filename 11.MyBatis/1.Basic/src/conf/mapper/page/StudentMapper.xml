<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.dao.page.StudentMapper">
    <select id="getStudentById" resultType="com.mybatis.bean.page.Student">
		SELECT * FROM student WHERE id = #{id};
	</select>

	<select id="getStudents" resultType="com.mybatis.bean.page.Student">
		SELECT * FROM student
	</select>

</mapper>